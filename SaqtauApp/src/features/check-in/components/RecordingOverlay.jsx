import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const RecordingOverlay = () => {
    return (
        <SafeAreaView>
            <Text>RecordingOverlay</Text>
        </SafeAreaView>
    );
};

export default RecordingOverlay;