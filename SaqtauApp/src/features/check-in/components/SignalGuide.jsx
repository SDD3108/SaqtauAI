import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const SignalGuide = () => {
    return (
        <SafeAreaView>
            <Text>SignalGuide</Text>
        </SafeAreaView>
    );
};

export default SignalGuide;