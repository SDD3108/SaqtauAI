import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const BeforeStartForm = () => {
    return (
        <SafeAreaView>
            <Text>BeforeStartForm</Text>
        </SafeAreaView>
    );
};

export default BeforeStartForm;