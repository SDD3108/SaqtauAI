import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const CaptureQualityCard = () => {
    return (
        <SafeAreaView>
            <Text>CaptureQualityCard</Text>
        </SafeAreaView>
    );
};

export default CaptureQualityCard;