import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const SegmentedControl = () => {
    return (
        <SafeAreaView>
            <Text>SegmentedControl</Text>
        </SafeAreaView>
    );
};

export default SegmentedControl;