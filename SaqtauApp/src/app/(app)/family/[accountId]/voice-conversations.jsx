import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const voiceConversations = () => {
    return (
        <SafeAreaView>
            <Text>voiceConversations</Text>
        </SafeAreaView>
    );
};

export default voiceConversations;