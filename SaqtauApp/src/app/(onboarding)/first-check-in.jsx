import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

const FirstCheckIn = () => {
    return (
        <SafeAreaView>
            <Text>FirstCheckIn</Text>
        </SafeAreaView>
    );
};

export default FirstCheckIn;